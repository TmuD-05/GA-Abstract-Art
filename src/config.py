"""
config.py

This module stores configuration parameters used throughout the system.

It includes settings for the genetic algorithm such as population size,
number of generations, mutation rate, number of shapes per image,
and image dimensions.

Centralizing these values makes it easier to tune and experiment
without modifying core logic.
"""